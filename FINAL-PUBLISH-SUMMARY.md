# ✅ Ready to Publish: @signaler/cli

## Package Details

- **Name**: `@signaler/cli` (much cleaner than `@signaler/signaler`!)
- **Version**: `1.0.8`
- **Files**: 117 files (~700KB)
- **Status**: ✅ Dry run passed

## To Publish

```bash
npx jsr publish
```

## After Publishing

Users can install with:

```bash
# Add to project
npx jsr add @signaler/cli

# Install globally  
npm install -g jsr:@signaler/cli

# Use it
signaler wizard
signaler audit
```

## Package URLs

- **JSR Page**: https://jsr.io/@signaler/cli
- **Import**: `jsr:@signaler/cli`

## Benefits of New Name

✅ **Shorter**: `@signaler/cli` vs `@signaler/signaler`  
✅ **Clearer**: "cli" indicates it's a command-line tool  
✅ **No repetition**: Avoids redundant "signaler/signaler"  
✅ **Professional**: Follows common naming patterns like `@angular/cli`

## Installation Comparison

```bash
# Old (repetitive)
npm install -g jsr:@signaler/signaler

# New (clean)
npm install -g jsr:@signaler/cli
```

## Ready to Publish!

The package is ready with the new, cleaner name. Just run:

```bash
npx jsr publish
```

🚀 Much better naming!