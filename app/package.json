{
  "name": "signaler-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm exec vite dev",
    "build": "pnpm exec svelte-kit sync && pnpm exec vite build",
    "preview": "pnpm exec vite preview",
    "sidecar:build": "tsx ../scripts/build-tauri-sidecar.ts",
    "tauri:dev": "pnpm sidecar:build && tauri dev",
    "tauri:build": "pnpm sidecar:build && tauri build"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.49.4",
    "@sveltejs/adapter-static": "^3.0.6",
    "@tauri-apps/cli": "2.9.1",
    "svelte": "^5.46.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vite": "^7.3.0",
    "@sveltejs/vite-plugin-svelte": "^6.2.3"
  },
  "dependencies": {
    "@tauri-apps/api": "2.9.1"
  }
}
