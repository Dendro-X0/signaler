# Auto-generated by Signaler CLI (Cloudflare Pages — Next on Pages)
# Install the builder:
#   pnpm add -D @cloudflare/next-on-pages
# Build locally:
#   npx @cloudflare/next-on-pages@1
# Deploy locally:
#   wrangler pages deploy .vercel/output/static --project-name signaler-cli-docs-site
name = "signaler-cli-docs-site"
pages_build_output_dir = ".vercel/output/static"
pages_functions_directory = ".vercel/output/functions"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
