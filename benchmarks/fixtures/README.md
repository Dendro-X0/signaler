# Benchmark fixtures (committed)

Stable snapshots used by `release:preflight`, gate validators, and tests. Regenerated intentionally via `pnpm run bench:sync-fixtures` after running gate evaluators (which write to gitignored `benchmarks/out/`).
